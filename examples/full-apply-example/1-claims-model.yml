version: v2-alpha
kind: Model
name: claims-model-new
model-type: python:3.6
payload:
  - /Users/bulat/Documents/Dev/Provectus/hydro-serving-example/models/apply-claims-model/src
contract:
  claim:
    inputs:
      client_profile:
        shape: [112]
        type: float32 # using tf type names instead of proto ones
        profile: text # data profiling
    outputs:
      amount:
        shape: scalar
        type: int64
        profile: real
